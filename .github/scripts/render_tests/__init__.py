"""Scoped test modules for the M2 renderer + generic backend.

The entry point remains ``.github/scripts/test_render.py`` (a thin runner). All test
modules share one ``failures`` list defined in :mod:`render_tests.harness`.
"""
