"""Scoped test modules for the M3 operator CLI (doctor / plan / apply / init).

The entry point remains ``.github/scripts/test_cli.py`` (a thin runner). All test
modules share one ``failures`` list defined in :mod:`cli_tests.harness`.
"""
